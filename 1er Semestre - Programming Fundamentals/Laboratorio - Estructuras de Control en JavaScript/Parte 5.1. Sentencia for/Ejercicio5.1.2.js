//Imprime los números pares entre 1 y 20.


/*El bucle irá incrementando el número por 2 cada vez. Esto lo hará hasta llegar al 20,
 ya que hasta ahí está perimitido que incremente. */
for(let i = 2; i <= 20; i += 2){
    //La consola imprimirá los números pares entre 1 y 20.
    console.log(i);
}