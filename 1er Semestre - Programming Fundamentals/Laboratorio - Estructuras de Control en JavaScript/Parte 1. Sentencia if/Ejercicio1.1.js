//Determina si un número es positivo.

let numeroIngresado = prompt ("Ingresa cualquier número.");
//Si el número ingresado es mayor o igual a cero,
if (numeroIngresado > 0){
    //la consola mostrará que es un número positivo.
    console.log(`${numeroIngresado} es un  número positivo.`);
}

//Si el número es menor que cero, no mostrará nada la consola. 