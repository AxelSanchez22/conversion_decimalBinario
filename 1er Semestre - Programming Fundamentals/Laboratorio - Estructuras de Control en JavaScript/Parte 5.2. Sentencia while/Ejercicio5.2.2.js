//Imprime los números pares entre 1 y 20.

//Se define la variable y se le da el valor de 2.
let numeroPar = 2
//Mientras el número sea menor o igual a 20,
while (numeroPar <= 20){
    //La consola imprimirá el valor del número.
    console.log(numeroPar);
    //El número se incrementará por dos cada ciclo hasta que llegue al límtite de 20.
    numeroPar += 2;
}