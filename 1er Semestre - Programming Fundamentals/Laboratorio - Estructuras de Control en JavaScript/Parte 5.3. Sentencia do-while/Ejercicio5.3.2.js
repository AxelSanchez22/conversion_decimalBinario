//Imprime los números pares entre 1 y 20.

//Se define una variable con el valor de 2.
let numero = 2;
//Se ejecuta el código primero y luego se evalúa la condición.
do{
    //Se imprime el valor de la variable.
    console.log(numero);
    //Se incrementa el valor de la variable por 2 cada ciclo que haga.
    numero += 2;
//Se repite el ciclo hasta que ya no cumpla la condición.
}while (numero <= 20);